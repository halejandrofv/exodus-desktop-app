# Install & Configuration
- github repo base boilerplate https://github.com/cawa-93/vite-electron-builder
- This will prompt you to choose a Vite template (Vue, React, Svelte, etc.) and set up the packages/renderer directory with the correct @app/renderer package name.
    npm run init

- install all package on workspaces
  npm i
- start project
  npm start

